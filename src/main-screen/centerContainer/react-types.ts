/**
 * ВНИМАНИЕ: Этот файл устарел и используется только для справки.
 * Не следует использовать его типы напрямую в приложении.
 * Вместо этого используйте стандартные типы из @types/react.
 * 
 * Файл оставлен для совместимости с существующим кодом.
 */

// Переименовано, чтобы избежать конфликтов с настоящими типами React
declare namespace _OldReactTypes {
  // Базовые типы для справки
  type Node = any;
  type Child = any;
  type Text = string | number;
  type Fragment = any;
  type NodeArray = Array<any>;
  type Key = string | number;
  
  // Остальные типы не используются
}

// Экспортируем пустой объект для совместимости с импортами
export const LegacyReactTypes = {}; 